[build]
  command = "pnpm add webmcp-proxy@latest && pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

[[edge_functions]]
  function = "cors-proxy"
  path = "/cors-proxy/*"
